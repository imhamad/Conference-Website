{#
/**
 * @file
 * Theme override to display a node.
 */
#}
{%
  set classes = [
    'node',
    'node-type-' ~ node.bundle|clean_class,
	'node-content',
	'feat-node-content',
    node.isPromoted() ? 'node--promoted',
    node.isSticky() ? 'node--sticky',
    not node.isPublished() ? 'node--unpublished',
  ]
%}

{% if teaser %}
<div{{ attributes.addClass('feat-container service-teaser') }}>
	{{ title_prefix }}
	{{ title_suffix }}
	<div class="feat-image">
	{% if content.field_image | render %}
	<div class="feat-img">{{ content.field_image }}</div>
	{% endif %}
	<div class="feat-content">
	<h5 class="margin-bottom-20"><a href="{{ url }}">{{ label }}</a></h5>
	<div class="feat-meta">
	<div class="feat-body">{{ content.body }}</div>
	<div class="post-link"><a href="{{ url }}" class="margin-top-20">More Details <i class="fa fa-angle-double-right"></i></a></div>
	</div>
	</div>
	</div>
</div>
{% else %}
<div{{ attributes.addClass(classes) }}>
	{{ title_prefix }}
	{{ title_suffix }}
		{% if content.field_image | render %}
		<div class="node-feat-image">{{ content.field_image }}</div>
		{% endif %}
		<h2 class="post-title">{{ label }}</h2>
		<div class="post-content">
		{{ content.body }}
		</div>
</div>
{% endif %}